@model VetClinicManager.DTOs.Visits.VisitCreateDto

@{
    ViewData["Title"] = "Rejestracja nowej wizyty";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h2>@ViewData["Title"]</h2>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="form-group mb-3">
                            <label asp-for="Title" class="control-label"></label>
                            <input asp-for="Title" class="form-control" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Description" class="control-label"></label>
                            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                        
                        @* Pole CreatedDate powinno być ustawiane automatycznie na serwerze,
                           więc nie powinno być w formularzu. Jeśli jest potrzebne,
                           można użyć pola 'datetime-local' *@

                        <div class="form-group mb-3">
                            <label asp-for="AnimalId" class="control-label"></label>
                            <select asp-for="AnimalId" class="form-control" asp-items="@ViewBag.Animals">
                                <option value="">-- Wybierz zwierzę --</option>
                            </select>
                            <span asp-validation-for="AnimalId" class="text-danger"></span>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6 form-group mb-3">
                                <label asp-for="Status" class="control-label"></label>
                                <select asp-for="Status" class="form-control" asp-items="@ViewBag.Statuses">
                                    <option value="">-- Wybierz status --</option>
                                </select>
                                <span asp-validation-for="Status" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 form-group mb-3">
                                <label asp-for="Priority" class="control-label"></label>
                                <select asp-for="Priority" class="form-control" asp-items="@ViewBag.Priorities">
                                    <option value="">-- Wybierz priorytet --</option>
                                </select>
                                <span asp-validation-for="Priority" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            @* ZMIANA: Usunięto tekst "Przypisz weterynarza" z etykiety. *@
                            <label asp-for="AssignedVetId" class="control-label"></label>
                            <select asp-for="AssignedVetId" class="form-control" asp-items="@ViewBag.Vets">
                                <option value="">-- Opcjonalnie, wybierz weterynarza --</option>
                            </select>
                            <span asp-validation-for="AssignedVetId" class="text-danger"></span>
                        </div>

                        <div class="form-group mt-4 d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">Powrót do listy</a>
                            <input type="submit" value="Zarejestruj" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}